class revive_script {
	title = "Whish revive system to use (only 'One Life Only' will be available when ACE is present)";
	values[] = {0, 1, 2};
	texts[] = {"One Life Only", "-= BTC =-", "TCB Wounding System"};
	default = 1;
};

#include "btc\_params.hpp"
